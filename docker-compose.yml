version: '3'
services:
  python:
    build: ./python
    container_name: youtubechecker-app
    ports:
      - "5000:80"
    environment:
      FLASK_APP: myapp.py
    volumes:
      - ./app:/workspace/app